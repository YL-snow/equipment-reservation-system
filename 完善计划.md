# equipment-reservation-system 完善计划

> 实验室设备预约与耗材管理系统 — 面向 PM/项目经理岗位简历

---

## 当前状态

| 维度 | 状态 | 详情 |
|------|------|------|
| 后端代码 | ✅ 完成 | Spring Boot 3.4.1 + Java 21，37 个 Java 文件，4 个 Controller |
| 前端代码 | ✅ 完成 | React 19 + TypeScript + Ant Design，6 个功能页面 |
| 数据库 | ✅ 完成 | MySQL 8.x，schema.sql + seed.sql |
| 文档 | ✅ 完整 | 需求说明、API 文档、验收报告 |
| Docker | ⚠️ 部分 | 有 Dockerfile，缺少 docker-compose |
| K8s | ✅ 完成 | deployment yaml 文件 |
| **目录结构** | **❌ 嵌套** | 前端源码在 `equipment-reservation-system/frontend/src/`，根目录 `frontend/` 只有构建产物 |
| **Git 仓库** | **❌ 无** | 需要初始化 |
| **README** | **❌ 待优化** | 需要面向 PM/项目经理岗位重新包装 |
| **LICENSE** | **❌ 缺失** | 需要补充 |
| **构建产物** | **❌ 未清理** | target/、dist/、node_modules/ 存在 |

---

## 阶段一：目录结构重构

### 当前问题

```
equipment-reservation-system/
├── backend/                     ← 后端源码（正常）
├── frontend/                    ← 只有构建产物（dist, node_modules, config）
├── database/                    ← SQL 脚本（正常）
├── docs/                        ← 文档（正常）
└── equipment-reservation-system/ ← 嵌套目录（多余）
    └── frontend/src/            ← 真正的前端源码在这里！
    └── k8s/                     ← K8s 部署文件
```

### 目标结构

```
equipment-reservation-system/
├── backend/                     ← Spring Boot 后端
│   └── src/main/java/com/ers/
├── frontend/                    ← React 前端（合并后的源码）
│   └── src/
│       ├── pages/               ← 6 个功能页面
│       ├── components/          ← 通用组件
│       ├── context/             ← React Context
│       └── api/                 ← API 调用
├── database/                    ← SQL 脚本
├── docs/                        ← 文档
├── k8s/                         ← K8s 部署
├── pom.xml (backend)
├── package.json (frontend)
├── docker-compose.yml           ← 新增
├── README.md
└── LICENSE
```

### 具体操作

1. 合并前端源码：将 `equipment-reservation-system/frontend/src/` 移动到根目录 `frontend/src/`
2. 确保根目录 `frontend/` 的 package.json、vite.config.ts 等配置文件完整
3. 删除嵌套的 `equipment-reservation-system/` 目录
4. 删除 `backend/target/`、`frontend/dist/`、`frontend/node_modules/` 等构建产物
5. 删除 `frontend/dist.zip` 等压缩包

---

## 阶段二：代码可用性验证

### 2.1 后端编译

```bash
cd backend
mvn clean compile -DskipTests
```

### 2.2 前端构建

```bash
cd frontend
pnpm install
pnpm run build
```

### 2.3 数据库脚本检查

- 检查 `database/schema.sql` 和 `database/seed.sql` 语法
- 确认建表顺序正确（外键依赖）

### 2.4 Docker Compose 补充

当前有 Dockerfile 但缺少 docker-compose.yml，需要补充一个编排文件，包含：
- MySQL 8.x
- Backend Spring Boot
- Frontend Nginx

---

## 阶段三：README & 简历文档优化

### README 内容规划

| 章节 | 内容 |
|------|------|
| 项目简介 | 一句话定义 + 解决的核心问题 |
| 产品价值 | 目标用户、核心场景、业务流程 |
| 核心功能 | 功能清单（设备浏览/预约/审批/库存） |
| 技术架构 | 架构图 + 技术栈表格 |
| 角色与权限 | ADMIN / STUDENT 的完整权限说明 |
| 项目管理 | 需求分析 → 接口设计 → 开发 → 部署 |
| 快速开始 | 数据库初始化 + 后端启动 + 前端启动 |
| 文档体系 | 需求文档 / API 文档 / 验收报告 |

---

## 阶段四：Git 仓库初始化

1. 在项目根目录初始化 git
2. 配置 `.gitignore`
3. 添加远程仓库
4. 首次 commit 并推送

---

## 阶段五：简历价值包装

提炼可直接放入简历的项目亮点，包括：
- 完整的业务系统（设备预约全流程）
- 前后端分离 + JWT 认证 + Docker 部署
- 文档驱动（需求/API/验收）
- 冲突检测 + 乐观锁等企业级特性

---

## 执行顺序

```
阶段一 (目录重构) → 阶段二 (代码验证) → 阶段三 (README)
     → 阶段四 (Git) → 阶段五 (简历包装)
```
